<%= form_for(@f) do |f| %>
  <% if @f.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@f.errors.count, "error") %> prohibited this f from being saved:</h2>

      <ul>
      <% @f.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
